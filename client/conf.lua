function love.conf(t)
    t.identity = "aldruna"
    t.window.title = "Aldruna"
    t.window.fullscreen = true
    t.window.fullscreentype = "desktop" -- uses the player's native resolution
    t.window.vsync = 1
    t.console = false
end
